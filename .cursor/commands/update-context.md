Follow core/slash-commands.md for command `/update-context`.

Read and execute: core/workflows/update-context-mapping.md

Use this when the orchestration context artifacts (spec/governance/agents/protocols)
exist, but they are stored in non-standard locations or filenames, and `/operate`
should be refreshed to use the new paths.

