Follow core/slash-commands.md for command `/update-ownership`.

Read and execute: core/workflows/update-ownership.md

Scan the repo (or a diff) against the fleet ownership map. Propose matrix
updates, confirm with the user, then write ownership-matrix (and light registry
owns notes). Do not create new agents or regenerate the whole pack.
