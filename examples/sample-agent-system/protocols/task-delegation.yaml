delegation:
  task_id: TASK-SAMPLE-001
  title: Define catalog list endpoint skeleton
  request_type: IMPLEMENTATION
  delegated_by: principal-software-architect
  primary_agent: backend-engineer
  supporting_agents: []
  reviewers:
    - principal-software-architect

  # Mandatory per-task disposable root — probes/builds/temp only here
  scratch_dir: agent-system/scratch/TASK-SAMPLE-001/
  handoff_dir: agent-system/handoffs/active/TASK-SAMPLE-001/

  objective: Propose a minimal list-catalog-items HTTP endpoint shape without selecting a cloud provider.
  business_context: Sample Catalog API demo; fictional greenfield.

  scope:
    included:
      - Route and response shape for listing catalog items
    excluded:
      - Auth, payments, UI, deployment
    repository_paths:
      - src/ (when present)

  acceptance_criteria:
    - Documented request/response fields for list items
    - Handoff completed with validation honesty

  contracts:
    consumed:
      - catalog-api
    produced: []
    changes_requiring_approval:
      - breaking field removals
    immutable: []

  dependencies:
    blocking: []
    non_blocking: []

  constraints:
    - Provider-neutral
    - No secrets
  quality_gates:
    - quality-gates.md
  required_approvals: []
  expected_artifacts:
    - API sketch or code under delegated paths (reference via path/URL in handoff)
  handoff_consumers:
    - principal-software-architect

  stop_conditions:
    - Missing approval for provider binding
  definition_of_done:
    - Thin handoff READY or BLOCKED under handoff_dir with clear next actions
    - No build trees or large dumps under handoffs/
    - Scratch used for any probes; Close will delete scratch_dir
