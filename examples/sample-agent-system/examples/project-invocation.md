# Project Invocation Example — Sample Catalog API

```text
/operate

Request: Propose a provider-neutral list endpoint for catalog items.
Authorization: planning and documentation only; no deployment; no secrets.
```

Architect loads `examples/sample-agent-system/` (or a copied `agent-system/`), selects `backend-engineer`, fills `protocols/task-delegation.yaml`, and integrates the handoff.
